{
  "name": "@mono/one",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "format:check": "prettier --ignore-path=../../.prettierignore --ignore-unknown --check .",
    "format": "prettier --ignore-path=../../.prettierignore --ignore-unknown --write .",
    "lint:check": "eslint . --ext .cjs,.js,.jsx,.mjs,.ts,.tsx",
    "lint": "eslint . --ext .cjs,.jsx,.mjs,.ts,.tsx --fix",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mono/components": "*"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
